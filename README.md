# YEP Updates

Public Sparkle update feed and signed DMG release assets for YEP.

The source code lives in the private YEP repository. This repository exists so
Sparkle can fetch `appcast.xml` and release DMGs over unauthenticated HTTPS.
